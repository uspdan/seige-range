# Default deny-all allowlist. Override per deployment by mounting a
# replacement file at /etc/tinyproxy/egress-allowlist.conf or rebuild
# the image with a project-specific copy. See egress-allowlist.conf.example.
